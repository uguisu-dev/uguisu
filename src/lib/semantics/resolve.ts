import { SyntaxNode } from '../syntax/node.js';
import { Symbol } from './symbol.js';

/**
 * resolve name nodes, and make semantic tree.
*/
export function resolve(syntaxTree: SyntaxNode, symbolTable: Map<SyntaxNode, Symbol>) {

}
