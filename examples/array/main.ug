fn main() {
    var x = [1, 2];

    console.write(numToStr(x[0]));
    console.write(numToStr(x[1]));

    x[1] = 10;
    addItem(x, 100);

    console.write(numToStr(x[0]));
    console.write(numToStr(x[1]));
    console.write(numToStr(x[2]));
}
