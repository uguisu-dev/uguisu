struct User {
    id: string,
    name: string,
    createdAt: number,
}

fn main() {
    var x = new User {
        id: "kani",
        name: "kani san",
        createdAt: 12345,
    };

    console.write(x.name);
}
